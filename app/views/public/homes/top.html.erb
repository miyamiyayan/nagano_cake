<div class="container-xl">

  <div class="row">
    <table class="table col-xl-2">
      <thead class="table-secondary">
        <tr>
          <th scope="col">ジャンル検索</th>
        </tr>
      </thead>
<!--ここから下が可変-->
      <tbody>
        <% @genres.each do |genre| %>
          <tr>
            <td><%= genre.name %></td>
          </tr>
        <% end %>
          <td>
            <div class="d-flex justify-content-center h6"><%= paginate @genres %></div>
          </td>
      </tbody>
    </table>
    <div class="col-xl-1"></div>
    <div class="col-xl-9 align-items-center">
      <p class="text-center">
        ようこそ、ながのCAKEへ！</br>
        このサイトは、ケーキ販売のECサイトとなります。</br>
        会員でない方も商品の閲覧はできますが、</br>
        購入には会員登録が必要になります。
      </p>
    </div>
  </div>

  <div class="row">
    <div class="col-xl-3"></div>
    <div class="col-xl-9 h2">新着商品</div>
  </div>

  <div class="row">
    <div class="col-xl-3"></div>
      <% @items.each do |item| %>
        <div class="col-xl-2">
          <div>
            <%= attachment_image_tag item, :image, :fill, 80, 60, format: 'jpeg' %>
          </div>
          <div>
            <%= link_to item_path(item.id) do %>
              <%= item.name %>
            <% end %>
            </br>
            <%= number_to_currency(item.with_tax_price,  :locale => 'jp') %>
          </div>
        </div>
      <% end %>
    <div class="col-xl-1">
      <%= link_to "商品一覧", items_path, class: "small" %>
    </div>
  </div>
  </br>
</div>